import React from "react";
import { Container } from "@mui/system";
import "./Contact.css";

function Contact() {
  return (
    <Container
      sx={{
        color: "white",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        height: "100%",
        border: "solid red",
      }}
    >
      CONTACT
    </Container>
  );
}

export default Contact;
